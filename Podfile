# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'thoughtCounter' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for thoughtCounter
	pod 'FSCalendar'

end

target 'thoughtCounter WatchKit App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for thoughtCounter WatchKit App

end

target 'thoughtCounter WatchKit Extension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for thoughtCounter WatchKit Extension

end
